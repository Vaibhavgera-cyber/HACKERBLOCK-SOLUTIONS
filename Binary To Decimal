#include<iostream>
#include<math.h>
using namespace std;
int main() {
	int n;
	cin>>n;
	int bin=0;
	int index=0; 
	while(n>0){
		bin+=(n%10)*pow(2,index);
		index++;
		n=n/10;
	}
	cout<<bin;
	return 0;
}
