#include <iostream>

using namespace std;

int main()
{
    int num,dig,rem,count=0;
    cin>>num;
    cin>>dig;
    while(num>0)
    {
        rem=num%10;
        if(rem==dig)
        {
            count++;
        }
        num=num/10;
    }
    cout<<count;
    

    return 0;
}
